# Shared

Shared project materials live here:

- `assets/`: brand and product assets used across platforms.
- `firmware/`: bridge dongle firmware that supports the Android and iOS paths.

Keep Android-specific source and build tooling in `Android/`, and iOS-specific source in `ios/`.
